{
  "name": "medieval-conquest",
  "version": "0.1.0",
  "description": "Medieval Conquest - Multiplayer medieval strategy game",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node server/src/server.js",
    "start": "npm run build && node server/src/server.js",
    "start:server": "node server/src/server.js",
    "postinstall": "cd server && npm install"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "phaser": "^3.80.1",
    "simplex-noise": "^4.0.3"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
